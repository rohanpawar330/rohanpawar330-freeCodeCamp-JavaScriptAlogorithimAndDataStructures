function splitify(str) {
  // Only change code below this line

  let splitArray = str.split(/\W/);
  console.log(splitArray)
  return splitArray;
  // Only change code above this line
}
splitify("Hello World,I-am code");
